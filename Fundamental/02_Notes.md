Stack Memory -> Memory allocated for storing local variables and function call information during program execution.

Heap Memory -> Memory allocated for dynamic memory allocation, where objects and data structures are stored during runtime.

Garbage Collection -> The process of automatically freeing up 
memory that is no longer in use by the program, typically managed by the Java Virtual Machine (JVM) in Java.x